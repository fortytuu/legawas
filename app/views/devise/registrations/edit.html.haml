%h1 Einstellungen
= devise_error_messages!
.col-md-4
  = form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }, class: 'form-inline') do |f|
    %h2 Account bearbeiten
    .form-group
      .label Username
      = f.text_field :username, autofocus: true, placeholder: 'Username', title: 'Gib deinen Username an.', class: 'form-control'
    .form-group
      .label E-Mail Adresse
      = f.email_field :email, autofocus: true, placeholder: 'E-Mail Adresse', title: 'Gib deine Email-Adresse an.', class: 'form-control'
    .form-group
      .label Passwort
      = f.password_field :password, autocomplete: "off", placeholder: 'Passwort', title: 'Bitte gib dein Passwort an.', class: 'form-control'
      .hint
        %i (Nur ausfüllen, wenn du dein Passwort ändern willst.)
    .form-group
      .label Passwort wiederholen
      = f.password_field :password_confirmation, autocomplete: "off", placeholder: 'Passwort wiederholen', title: 'Bitte wiederhole dein Passwort', class: 'form-control'
    .form-group
      .label Dein aktuelles Passwort
      = f.password_field :current_password, autocomplete: "off", placeholder: 'Dein aktuelles Passwort', title: 'Bitte gib dein aktuelles Passwort an um die Änderungen zu speichern.', class: 'form-control'
      .hint
        %i (Bitte gib dein aktuelles Passwort ein, um die Änderungen zu bestätigen.)
    .actions
      = f.submit "Ändern", class: 'btn btn-primary'
.col-md-4
  %h2 Account löschen
  %p
    Unzufrieden? #{button_to "Account löschen", registration_path(resource_name), class: 'btn btn-primary', data: { confirm: "Willst du deinen Account wirklich löschen?" }, method: :delete}
  = link_to "Zurück", :back